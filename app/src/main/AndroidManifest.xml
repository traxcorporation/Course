<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ytraore.listedecourse"
    android:versionCode="1"
    android:versionName="1.0">


    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>

    <application

    android:allowBackup="true"
    android:icon="@drawable/ic_launcher"
    android:label="@string/app_name"
    android:theme="@style/AppTheme">

    <activity
        android:name=".AccueilCourse"
        android:label="@string/app_name" >
        <intent-filter>

            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

    </activity>



    <activity
        android:name=".MainActivity"
        android:label="@string/app_name">
    </activity>

    <activity
        android:name=".RubriqueFruits"
        android:label="Choisir un fruit:">

    </activity>

    <activity
        android:name=".RubriqueLaitiers"
        android:label="Choisir des produits laitiers">

    </activity>


    <activity
        android:name=".RubriqueLegumes"
        android:label="Choisir des legumes:">

    </activity>

    <activity
        android:name=".RubriqueBoucherie"
        android:label="Choisir dans la boucherie:">
    </activity>

    <activity
        android:name=".RubriquePoissonerie"
        android:label="Choisir dans la poissonnerie:">
    </activity>

    <activity
        android:name=".RubriqueBoulangerie"
        android:label="Choisir dans la boulangerie:">
    </activity>

    <activity
        android:name=".RubriqueCharcuterie"
        android:label="Choisir dans la charcuterie:">

    </activity>



        <activity
            android:name=".Bieres"
            android:label="@string/app_name">

        </activity>

    </application>


</manifest>
